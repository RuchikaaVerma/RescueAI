spring:
  datasource:
    url: jdbc:h2:mem:rescueai_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  ai:
    openai:
      api-key: test-key
rescueai:
  security:
    jwt:
      secret: test-secret-test-secret-test-secret-test-secret
