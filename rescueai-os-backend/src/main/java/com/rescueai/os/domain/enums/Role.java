package com.rescueai.os.domain.enums;

/** Every actor in the platform maps to exactly one role. Authorization is role-based. */
public enum Role {
    CITIZEN,
    HOSPITAL,
    NGO,
    GOVERNMENT,
    VOLUNTEER,
    COMMAND_CENTER,
    SYSTEM_ADMIN
}
